{
  "name": "access-passes-server",
  "version": "1.0.0",
  "private": true,
  "description": "Shared backend + app hosting for the Access Passes key-card system",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "pg": "^8.11.5"
  }
}
